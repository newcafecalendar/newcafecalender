# 新商品情報の自動収集設定

サイトは引き続き、公開用GoogleスプレッドシートのCSVを読み込みます。自動化は、公式記事を「確認待ち」へ集め、内容を確認したものだけ公開用シートに送る方式です。

## 初回設定

1. 現在サイトが参照しているGoogleスプレッドシートを開きます。
2. **拡張機能 → Apps Script** を開きます。
3. `automation/Code.gs` の内容を貼り付けて保存します。
4. `setupSheets()` を1回実行して権限を許可します。
5. `createDailyTrigger()` を1回実行します。毎朝7時ごろに公式記事を収集します。

## 日々の運用

1. 「確認待ち」シートに新しい記事候補が入ります。
2. 記事を開き、商品ごとに発売日・カテゴリ（`drink` / `food` / `other`）・商品名・説明を確認します。
3. 公開したい行の「公開」にチェックを入れます。
4. メニューの **カフェカレンダー → 承認済み商品を公開用シートへ反映** を選びます。

公開用シートは、サイトで使うCSVの列順を維持します。

| 登録日時 | 発売日 | ブランド | カテゴリ | 商品名 | 説明 | 公式URL |
| --- | --- | --- | --- | --- | --- | --- |

## 注意

- 公式ページのHTML構造や掲載方針が変わった場合、取得ロジックの調整が必要です。
- 取得候補には店舗オープンやキャンペーンも混ざるため、公開前に必ず確認します。
- シート名が既存の公開シートと異なる場合は、`CONFIG.publishedSheetName` を現在のシート名へ変更してください。
